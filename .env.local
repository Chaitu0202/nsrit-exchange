# Database URL (replace with actual if you're using PostgreSQL, MongoDB, etc.)
DATABASE_URL="mongodb://localhost:27017/nsritexchange"  # or your cloud DB

# JWT secret (used for user auth / token signing)
JWT_SECRET="super-secure-secret-key"

# Public app URL (used in emails, redirects, etc.)
NEXT_PUBLIC_APP_URL="https://nsritexchange.vercel.app"

# Stripe/Payment keys (optional for future use)
# STRIPE_SECRET_KEY="your-stripe-secret"
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="your-stripe-publishable-key"
